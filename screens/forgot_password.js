import {
  StyleSheet,
  Text,
  TextInput,
  View,
  TouchableOpacity,
} from "react-native";

const Forgot_Password = ({ navigation }) => {
  return <View></View>;
};

export default Forgot_Password;
